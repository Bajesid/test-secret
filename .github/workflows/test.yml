---
name: Test secret

on:
  push:
    branches:
      - main

jobs:
  build:
    env:
      my_secret: ${{ secrets.SECRET }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: echo SECRET
      if: $my_secret == 'test value'
